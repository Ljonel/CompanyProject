import * as React from "react";
import styled from "styled-components";

export interface PeoplePageProps {}

const PeoplePage: React.SFC<PeoplePageProps> = () => {
  return (
    <div>
      <h1>People Page</h1>
    </div>
  );
};

export default PeoplePage;
