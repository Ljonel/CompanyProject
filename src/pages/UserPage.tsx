import * as React from "react";
import styled from "styled-components";

export interface UserPageProps {}

const UserPage: React.SFC<UserPageProps> = () => {
  return (
    <div>
      <h1>user xd Page</h1>
    </div>
  );
};

export default UserPage;
