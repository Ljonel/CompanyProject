import * as React from "react";
import styled from "styled-components";

export interface EntitiesPageProps {}

const EntitiesPage: React.SFC<EntitiesPageProps> = () => {
  return (
    <div>
      <h1>Entities Page</h1>
    </div>
  );
};

export default EntitiesPage;
