import React from "react";
import "./App.css";
import MainPage from "./MainPage/MainPage";

const App = () => {
  // const [actuallIcon, setIcon] = useState("house2");
  // const changeActuallIcon = (name) => {
  //   setIcon(name);
  //   console.log(name);
  // };

  return (
    <div className="App">
      <MainPage />
    </div>
  );
};

export default App;
